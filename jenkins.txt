pipeline {
    agent any

    environment {
        NODE_ENV = 'production'
        MONGO_URI = credentials('mongo-uri')
        SLACK_WEBHOOK = credentials('slack-webhook')
        RENDER_URL = 'https://darkroom-app-2khw.onrender.com'
    }

    stages {
        stage('Install') {
            steps {
                sh 'npm install'
            }
        }
        stage('Test') {
            steps {
                sh 'npm test'
            }
        }
        stage('Deploy') {
            steps {
                // Render deploy command or script if needed
                echo 'Deploy step placeholder'
            }
        }
    }

    post {
        success {
            slackSend(
                channel: '#Joan_IP1',
                message: "✅ Build #${env.BUILD_ID} passed. Site is live at: ${env.RENDER_URL}"
            )
        }
        failure {
            slackSend(
                channel: '#Joan_IP1',
                message: "❌ Build #${env.BUILD_ID} failed."
            )
        }
    }
}
